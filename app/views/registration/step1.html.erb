<h1 class="header">Register: Step 1</h1>
<p>Select an account name. This will be used to identify your content in the site 
and form part of the URL</p>

<p>Account names can only contain letters, numbers and underscores (_) and must start with a letter. They must be at least 6 characters long. 
</p>


<%= render "shared/simple_error_messages", :target => @registration_step1_command %>
<%= form_for(@registration_step1_command, :url => {:action => 'step1_complete'} ) do |f| %>
<table class="form">
 <tr>
   <td class="label"><%= f.label :account_name, 'Your account name:' %></td>
   <td class="field"><%= f.text_field :account_name, :class => 'initial_focus'%></td>
 </tr>
</table>
<%= f.submit :value => 'Continue', :class => 'submit default_button' %>
<% end %>

