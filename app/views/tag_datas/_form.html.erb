<%= form_for(@tag_data) do |f| %>
  <% if @tag_data.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tag_data.errors.count, "error") %> prohibited this tag_data from being saved:</h2>

      <ul>
      <% @tag_data.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
